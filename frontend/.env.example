# Frontend Environment Configuration
# Copy this file to .env.local and fill in the actual values

# API Configuration
# Keep the `/api` suffix so local Vite proxy and Docker Nginx both work the same way.
VITE_API_URL=/api
